export let Swing = [];
export let Swang = [];

for (let i = 0; i <= 75; i++) {
  let num = i * 2;
  // let num;
  num <= 50 && Swang.push(num);

  Swing.push(num);
}
for (let i = 74; i >= 0; i--) {
  let num = i * 2;
  // let num;

  num <= 50 && Swang.push(num);

  Swing.push(num);
}
